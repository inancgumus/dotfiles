# PYTHON
# eval "$(pyenv init -)"
# eval "$(pyenv virtualenv-init -)"
## prompt changing will be removed from future release.
## configure `export PYENV_VIRTUALENV_DISABLE_PROMPT=1' to simulate
## the behavior.

export PYENV_VIRTUALENV_DISABLE_PROMPT=1
